import './globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Gummy — Collaborative Rooms',
  description: 'Join collaborative rooms with fair queue, live streaming, and thread isolation. Share URLs, collaborate in real-time, and experience seamless multi-user interactions.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
